# Playwright E2E Testing Environment Variables
# Copy this file to .env.test and customize if needed

# Base URL for the application (defaults to http://localhost:8080 if not set)
PLAYWRIGHT_BASE_URL=http://localhost:8080

# Note: Test user credentials are automatically generated
# Each test run creates a new user with a unique email (test-{timestamp}@example.com)
# No need to manually create test users in Supabase!
